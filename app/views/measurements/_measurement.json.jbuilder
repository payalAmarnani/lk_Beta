json.extract! measurement, :id, :Customer_id, :chest, :neck, :waist, :hip, :shirt_length, :sleeve_length, :back, :wrist, :sleeve_width, :shoulder_type, :created_at, :updated_at
json.url measurement_url(measurement, format: :json)
